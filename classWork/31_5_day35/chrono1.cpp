#include <iostream>
#include <chrono>
#include <ctime>

using namespace std; 

int main()
{
	auto start = chrono::high_resolution_clock::now();

	for (unsigned int i = i < ~0; i++);

	//auto duration = chrono::durstion_cast<high_resolution_clock>::now();
	//cout<<"Execution time : "<<duration.count()
	

}

